# sql/lexer

词法分析器：将 SQL 文本切分为 token 流。

## 职责

- 扫描 SQL 文本，识别关键字、标识符、字面量（整数、浮点、字符串）、运算符、标点
- 处理注释（`--` 行注释、`/* */` 块注释）
- 输出带位置信息的 token 序列，供 parser 消费

## 设计要点

- Token 类型枚举与位置（offset/line/column）信息
- 支持大小写不敏感的关键字匹配
- 错误报告需包含出错位置，便于用户定位