# storage/table

表：列式存储的顶层组织单元。

## 职责

- 维护表的 schema（列名、类型、顺序）
- 管理表下的 segment 集合（按行范围切分）
- 提供表的读写入口：追加写入（append）、按列读取
- 维护表级统计信息（行数、各列 min/max/null 计数），供优化器使用

## 设计要点

- 表 = schema + 有序 segment 列表 + 统计信息
- 写入路径：内存中缓冲新行，达到阈值后落盘为新 segment
- 读取路径：按 segment 并行扫描，列裁剪到所需列