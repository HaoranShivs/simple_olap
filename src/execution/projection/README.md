# execution/projection

投影算子：列选择与表达式计算。

## 职责

- 列选择：从输入 `Batch` 中挑选输出列（零拷贝引用）
- 表达式计算：对 SELECT 列表中的表达式（算术、比较、函数调用）做向量化求值
- 处理别名与输出列顺序

## 设计要点

- 纯列选择走零拷贝路径，仅表达式列需要物化新列
- 表达式求值复用 vector 的向量化类型，逐批计算
- 与 filter 的 selection 配合：仅在选中行上计算表达式